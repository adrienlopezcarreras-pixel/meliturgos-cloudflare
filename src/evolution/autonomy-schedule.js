// Canonical schedule advertised by MEL's autonomy state API.
// Keep this value aligned with wrangler.jsonc; the wiring test enforces it.
export const AUTONOMY_RUNTIME_CRON = '* * * * *';

export default AUTONOMY_RUNTIME_CRON;
