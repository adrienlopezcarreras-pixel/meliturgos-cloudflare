import { migrate } from '../persistence/migrations.js';
let fallback=null;
export class D1BridgeRepository { constructor(db){this.db=db;this.ready=null;} async init(){if(this.db){if(!this.ready)this.ready=migrate(this.db);await this.ready;}} async heartbeat(status='ONLINE',metadata={}){await this.init();const t=Date.now();if(!this.db){fallback={bridge_id:'primary',last_seen:t,status,metadata};return fallback;}await this.db.prepare("INSERT INTO dev_bridge_state(bridge_id,last_seen,status,metadata_json) VALUES('primary',?,?,?) ON CONFLICT(bridge_id) DO UPDATE SET last_seen=excluded.last_seen,status=excluded.status,metadata_json=excluded.metadata_json").bind(t,status,JSON.stringify(metadata)).run();return {bridge_id:'primary',last_seen:t,status};} async status(){await this.init();const r=this.db?await this.db.prepare("SELECT * FROM dev_bridge_state WHERE bridge_id='primary'").first():fallback;if(!r)return {online:false,last_seen:null,status:'OFFLINE'};return {online:Date.now()-Number(r.last_seen)<60000,last_seen:Number(r.last_seen),status:r.status};} }
