import { enhanceFullModeControls } from './full-mode-control-enhancer.js';

export const MVP_BEHAVIOR_PATCH = `<style id="mel-mvp-behavior-style">
.mel-title{margin:0 0 4px;text-align:center;font-size:clamp(1.45rem,4vw,2rem);letter-spacing:.12em;font-weight:850;color:#fff;text-shadow:0 2px 18px rgba(0,0,0,.45)}
.mel-mode-label{margin:0 0 9px;text-align:center;font-size:.72rem;letter-spacing:.12em;font-weight:800;color:rgba(255,255,255,.8);text-transform:uppercase}
.app:before,.app:after{display:none!important;content:none!important}.theme-switch{display:none!important}.mel-bottom-tools .theme-switch{display:block!important;position:relative!important;top:auto!important;left:auto!important;z-index:50!important;width:max-content!important}.mel-bottom-tools .theme-orb{width:44px!important;height:44px!important;font-size:20px!important}.mel-bottom-tools .theme-panel{top:auto!important;bottom:52px!important;left:0!important;max-height:min(68vh,560px)!important}
#voiceStatus.mel-idle-voice{display:none!important}.controls{display:grid!important;grid-template-columns:1fr!important;max-width:none!important}.controls #send{width:100%!important}.drop{display:block!important}#fileInput{display:none!important}.mel-bottom-tools{display:grid;grid-template-columns:1fr;gap:9px;align-items:start;margin:12px 0 0}.mel-bottom-tools #full{grid-column:1/-1;width:100%;min-height:46px;font-weight:800}
.mel-continue-row,.mel-recall-row{min-height:22px;margin:2px 2px 4px;display:flex;align-items:center;gap:10px;flex-wrap:wrap}.mel-continue-link,.mel-recall-link{color:var(--accent);font-size:.82rem;font-weight:750;text-decoration:underline;cursor:pointer}.mel-continue-link{display:none}.mel-continue-link.visible{display:inline}.mel-recall-link[aria-busy="true"]{opacity:.55;pointer-events:none}
@media(max-width:700px){.mel-title{font-size:1.35rem}.mel-bottom-tools{gap:7px}}
</style><script id="mel-mvp-behavior-runtime">
(function(){
  const CHAT_TIMEOUT_MS=120000,CHAT_ATTEMPTS=2,RETRYABLE=new Set([502,503,504]);
  const CONTINUE_TEXT='Continue exactement à partir de ta dernière phrase, sans répéter ce qui précède. Termine complètement ta réponse.';
  function timeoutSignal(){try{return AbortSignal?.timeout?AbortSignal.timeout(CHAT_TIMEOUT_MS):undefined}catch{return undefined}}
  function patchChatTimeout(){if(window.__melLongChatFetchPatched||!window.fetch)return;const nativeFetch=window.fetch.bind(window);window.__melLongChatFetchPatched=true;window.fetch=async function(input,init){const url=typeof input==='string'?input:String(input?.url||'');if(!url.includes('/api/chat'))return nativeFetch(input,init);let last;for(let attempt=1;attempt<=CHAT_ATTEMPTS;attempt++){const next={...(init||{})};const signal=timeoutSignal();if(signal)next.signal=signal;try{const response=await nativeFetch(input,next);if(!RETRYABLE.has(response.status)||attempt===CHAT_ATTEMPTS)return response;last=new Error('HTTP_'+response.status)}catch(error){last=error;if(attempt===CHAT_ATTEMPTS)throw error}}throw last||new Error('CHAT_RETRY_EXHAUSTED')}}
  function normalizeUserLabels(root=document){root.querySelectorAll?.('.who').forEach(node=>{const text=String(node.textContent||'');if(/^Vous(?:\\s*·.*)?$/i.test(text))node.textContent=text.replace(/^Vous/i,'Adrien')})}
  function installMinimalLayout(){const app=document.querySelector('.app'),avatar=document.querySelector('.avatar-wrap'),windowPanel=document.querySelector('.window'),full=document.getElementById('full');if(!app||!avatar)return;if(!document.getElementById('melTitle')){const title=document.createElement('h1');title.id='melTitle';title.className='mel-title';title.textContent='MEL';const mode=document.createElement('div');mode.className='mel-mode-label';mode.textContent='IA + Développement';app.insertBefore(title,avatar);title.insertAdjacentElement('afterend',mode)}if(windowPanel&&full){let bottom=document.getElementById('melBottomTools');if(!bottom){bottom=document.createElement('div');bottom.id='melBottomTools';bottom.className='mel-bottom-tools';windowPanel.insertAdjacentElement('afterend',bottom)}const themeSwitch=document.querySelector('.theme-switch');if(themeSwitch&&!bottom.contains(themeSwitch))bottom.appendChild(themeSwitch);if(!bottom.contains(full))bottom.appendChild(full)}}
  function syncVoiceStatus(){const node=document.getElementById('voiceStatus');if(!node)return;const refresh=()=>{const text=String(node.textContent||'').trim();node.classList.toggle('mel-idle-voice',/^(Touchez son visage pour parler|Reconnaissance vocale non disponible dans ce navigateur\.?|)$/i.test(text))};new MutationObserver(refresh).observe(node,{childList:true,subtree:true,characterData:true});refresh()}
  function addMessage(role,text){const messages=document.getElementById('messages');if(!messages)return;messages.querySelector('.empty')?.remove();const node=document.createElement('div');node.className='msg '+(role==='user'?'user':'mel');const who=document.createElement('span');who.className='who';who.textContent=role==='user'?'Adrien':'MEL';const body=document.createElement('div');body.textContent=String(text||'');node.append(who,body);messages.appendChild(node)}
  async function restoreLatest(){const id=localStorage.getItem('mel.conversation');if(!id)return;const status=document.getElementById('status');try{const r=await fetch('/api/gen2/conversations/messages?conversation_id='+encodeURIComponent(id),{cache:'no-store',credentials:'same-origin'});if(!r.ok)throw new Error('HTTP_'+r.status);const data=await r.json(),rows=Array.isArray(data.messages)?data.messages:[],messages=document.getElementById('messages');if(messages)messages.innerHTML='';for(const row of rows.slice(-30)){const role=String(row?.role||'').toLowerCase();if(role==='user'||role==='assistant'||role==='mel')addMessage(role==='user'?'user':'mel',row?.content||row?.text||'')}if(status)status.textContent=rows.length?'Dernière conversation reprise.':'Conversation reprise.';normalizeUserLabels(messages)}catch(e){if(status)status.textContent='Reprise impossible : '+(e?.message||'ERREUR')}}
  async function resumeLatestChat(link){const status=document.getElementById('status');link?.setAttribute('aria-busy','true');try{const r=await fetch('/api/gen2/conversations',{cache:'no-store',credentials:'same-origin'});if(!r.ok)throw new Error('HTTP_'+r.status);const data=await r.json(),rows=Array.isArray(data.conversations)?data.conversations:[];const stamp=v=>{const n=Number(v);if(Number.isFinite(n)&&n>0)return n;const d=Date.parse(String(v||''));return Number.isFinite(d)?d:0};rows.sort((a,b)=>stamp(b.updated_at||b.created_at)-stamp(a.updated_at||a.created_at));const latest=rows[0];if(!latest?.id)throw new Error('AUCUNE_CONVERSATION');localStorage.setItem('mel.conversation',String(latest.id));if(status)status.textContent='Reprise du dernier chat…';await restoreLatest()}catch(e){if(status)status.textContent='Reprise impossible : '+(e?.message||'ERREUR')}finally{link?.setAttribute('aria-busy','false')}}
  function installConversationLinks(){const input=document.getElementById('input'),send=document.getElementById('send'),messages=document.getElementById('messages');if(!input||!send||!messages||document.getElementById('melRecallLatest'))return;const row=document.createElement('div');row.className='mel-recall-row';const recall=document.createElement('span');recall.id='melRecallLatest';recall.className='mel-recall-link';recall.tabIndex=0;recall.setAttribute('role','link');recall.textContent='Reprendre la dernière conversation';const cont=document.createElement('span');cont.id='melContinueLink';cont.className='mel-continue-link';cont.tabIndex=0;cont.setAttribute('role','link');cont.textContent='Continuer depuis la dernière phrase';row.append(recall,cont);input.insertAdjacentElement('afterend',row);const runRecall=()=>resumeLatestChat(recall);recall.onclick=runRecall;recall.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();runRecall()}};const runContinue=()=>{input.value=CONTINUE_TEXT;input.dispatchEvent(new Event('input',{bubbles:true}));send.click()};cont.onclick=runContinue;cont.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();runContinue()}};const refresh=()=>{normalizeUserLabels(messages);cont.classList.toggle('visible',Boolean(messages.querySelector('.msg.mel')))};new MutationObserver(refresh).observe(messages,{childList:true,subtree:true,characterData:true});refresh()}
  function init(){patchChatTimeout();installMinimalLayout();syncVoiceStatus();installConversationLinks();normalizeUserLabels()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
</script>`;

export async function enhanceMvpBehavior(response) {
  if (!(response instanceof Response)) return response;
  const type=response.headers.get('content-type')||'';if(!type.includes('text/html'))return response;
  const html=await response.text();
  if(html.includes('data-panel="chat"')&&html.includes('id="chatInput"')){
    return enhanceFullModeControls(new Response(html,{status:response.status,statusText:response.statusText,headers:response.headers}));
  }
  if(!html.includes('id="input"'))return new Response(html,{status:response.status,statusText:response.statusText,headers:response.headers});
  if(html.includes('mel-mvp-behavior-runtime'))return new Response(html,{status:response.status,statusText:response.statusText,headers:response.headers});
  const body=html.includes('</body>')?html.replace('</body>',`${MVP_BEHAVIOR_PATCH}</body>`):html+MVP_BEHAVIOR_PATCH;const headers=new Headers(response.headers);headers.set('content-length',String(new TextEncoder().encode(body).length));return new Response(body,{status:response.status,statusText:response.statusText,headers});
}
