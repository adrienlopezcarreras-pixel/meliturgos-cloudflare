MELITURGOS factual checkpoint — GEN2-17 supervised autonomy cycles
UTC: 2026-09-12T19:44Z
Branch: candidate/mel-clean-autonomy
Verified HEAD before checkpoint: a8375bbd13cce0ab8ce8127e7640570a1af654f0
Roadmap item: GEN2-17 — Dev Agent / auto-évolution supervisée

FICHIERS
- src/evolution/autonomy-supervisor.js (existing; reused, not duplicated)
- tests/autonomy-supervisor.test.mjs (existing; reused)
- tests/autonomy-ready-for-review-recovery.test.mjs (existing; branch fixture converged)
- tests/owner-development-queue.test.mjs (existing; branch fixture converged)
- teacher-bridge/replies.jsonl (existing Teacher approval evidence)
- this checkpoint file (existing factual record reused)

CHANGEMENTS / PROGRESS VERIFIED
- Canonical candidate is candidate/mel-clean-autonomy.
- Recent commits e931ab937d191165ab7dcce8e2e8ee33b8b1f0b6 and a8375bbd13cce0ab8ce8127e7640570a1af654f0 converged recovery and owner-queue test fixtures away from candidate/augmentio-core and onto candidate/mel-clean-autonomy.
- AutonomySupervisor already contains the anti-starvation rule required by the Teacher approval for GEN2-17: WAITING_TEACHER owner work stays persisted and untouched, while an already-actionable internal supervised job may advance; actionable owner work still outranks actionable background work.
- Existing supervisor tests cover: an owner WAITING_TEACHER job not starving an approved internal job; creation of the next internal roadmap job while passive owner review remains untouched; two consecutive autonomous roadmap transitions with the owner request still WAITING_TEACHER; and actionable owner work retaining priority.
- Teacher reply for request 1cdedf2f-d98b-43fd-ac6c-75a3e17ae8f7 APPROVE_PLAN explicitly authorized this candidate-only GEN2-17 direction and required repeated supervised cycles without mutating passive owner work.
- full-candidate-ci run 34714711966 completed SUCCESS on exact HEAD a8375bbd13cce0ab8ce8127e7640570a1af654f0 after the canonical-branch fixture convergence.

REUTILISATION
- No second orchestrator, roadmap, Work DAG, Council, or Teacher channel was introduced.
- The existing AutonomySupervisor, persistent dev-job repository, Teacher bridge, Council/preflight, Work DAG, and canonical full-candidate-ci remain the single path.

TESTS
- full-candidate-ci: SUCCESS on exact SHA a8375bbd13cce0ab8ce8127e7640570a1af654f0 (run 34714711966).
- Supervisor coverage already proves two consecutive roadmap transitions while passive owner review remains untouched.
- Canonical branch convergence fixtures are now aligned in recovery and owner queue tests.

RISQUES
- Unit/integration evidence is not by itself proof of two fresh live production-runtime development cycles on the deployed Worker.
- The configured Council role coverage is code/test proven, but the prior Council checkpoint explicitly does not claim new external live-provider invocation from this tool surface.
- MEL_TEACHER_APPROVED_BRANCH convergence should remain fail-closed wherever an approved-branch environment value is consumed; no new completion claim is made here without runtime proof.

ROLLBACK
- This checkpoint is documentation-only and can be reverted as one candidate commit.
- No production deployment, DNS/auth/billing/permission change, secret write, destructive D1 migration, or paid/unknown-cost provider was used.

CRITERES_DE_FIN
GEN2-17 must NOT be marked DONE solely from this checkpoint. Completion still requires factual evidence that at least two consecutive supervised development cycles execute through the real runtime on the canonical branch, each retaining request/SHA provenance, preserving passive owner WAITING_TEACHER work, passing targeted tests and full-candidate-ci on the exact resulting SHA, with no production privilege widening.

NEXT SAFE STEP
Use the existing runtime/Teacher/Council path to capture the first and second real consecutive GEN2-17 supervised-cycle completions on candidate/mel-clean-autonomy. If branch configuration or Teacher-approved branch diverges, fail closed and request a fresh review rather than creating a fallback branch.

---
RUN UPDATE 2026-09-12T21:52Z

FICHIERS
- teacher-bridge/replies.jsonl (existing file extended; no new Teacher channel)
- teacher-bridge/checkpoints/gen2-17-supervised-cycles-20260912T1944Z.txt (this existing checkpoint extended)

CHANGEMENTS
- Live runtime Teacher smoke run 34717613229 exposed five pending owner review requests. Three referenced noncanonical candidate/augmentio-core; two referenced canonical candidate/mel-clean-autonomy but stale SHAs.
- Added correlated Teacher replies for all five runtime requests with provenance teacher=chatgpt-automation-zero-euro: noncanonical requests were REJECT; canonical-but-stale requests were NEEDS_CHANGES. No owner request was approved without inspectable scope, patch or test evidence.
- The same smoke evidence showed the internal GEN2-17 job still TEACHER_APPROVED against an earlier Teacher request and reported implementation_proposal_ready=false. Existing autonomy-runtime.js already contains stale-approval requeue logic; no parallel orchestrator or workaround was added.
- Current Council status exposed by the live smoke contains two Workers AI responses but role fields are null and all_required_roles_satisfied=false for the persisted internal job. Existing src/teachers/augmentio-council.js already implements mandatory architecture, security, tests and product/integration roles plus MEL synthesis for a fresh preflight, so the correct next action is to force a fresh canonical Teacher/preflight cycle through existing runtime logic rather than create a new Council.

REUTILISATION
- Reused existing Teacher replies JSONL, canonical branch policy, autonomy runtime stale-SHA fail-closed behavior, Augmentio Council role implementation and existing checkpoint.
- No second roadmap, candidate branch, Council, orchestrator or development path created.

TESTS
- The Teacher reply commit was 30dc88055b35d22a9af57391463f7b04f26fcbec.
- full-candidate-ci run 34721185582 completed SUCCESS on exact SHA 30dc88055b35d22a9af57391463f7b04f26fcbec, including dependency security gate, syntax and full test suite.

RISQUES
- The live Worker has not yet demonstrated reconciliation of these five new Teacher replies; that requires a subsequent runtime tick/smoke observation.
- The internal GEN2-17 approval predates the latest canonical HEAD, so implementation must fail closed/requeue on stale SHA before any plan/code generation.
- Current persisted Council evidence for GEN2-17 does not prove mandatory role coverage; a fresh preflight must use the existing role-based Council implementation and preserve provenance.

ROLLBACK
- Revert commit 30dc88055b35d22a9af57391463f7b04f26fcbec to remove only the newly appended Teacher decisions.
- No production deployment, DNS/auth/billing/permission change, destructive D1 operation, paid provider or secret write occurred.

CRITERES_DE_FIN
- A later live runtime observation must show the five pending replies reconciled or safely requeued according to verdict.
- GEN2-17 must obtain a fresh Teacher request bound to the then-current canonical HEAD if the old approval is stale.
- Fresh Council evidence must show required roles architecture/security/tests/product-integration with MEL synthesis and zero-added-cost provenance.
- Implementation may proceed only after exact request/SHA correlation, targeted tests and full-candidate-ci on the exact resulting SHA.
