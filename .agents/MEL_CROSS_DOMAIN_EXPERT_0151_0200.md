# MEL Cross-domain Expert — cycles 151–200

Checkpoint: `MEL_CROSS_DOMAIN_EXPERT_PLUS_cycle_200/10000`. Additive research corpus; not implementation proof. A lesson becomes validated XP only after code application + test/runtime evidence under `.agents/XP_PROTOCOL.md`.

## Provenance
Primary/current references reviewed 2026-09-21: Model Context Protocol specification/ecosystem guidance on tool schemas, structured output, annotations and authorization; Cloudflare D1 Sessions/read replication/Time Travel/index guidance; Cloudflare R2 consistency/Workers API; Cloudflare Queues batching/retries/DLQ; Durable Objects alarms/best practices; Cloudflare Workflows rules/overview (updated through 2026-09-18); GitHub artifact attestations/Sigstore guidance. No `MEL_TRANSFER_*_10000.md` package was discoverable by repository search at this checkpoint. Sources are research provenance, not evidence that MEL already implements the lesson.

## Cycles 151–200
151. MCP input contracts → tools publish narrow machine-validatable schemas → schema validity does not establish semantic authorization → malformed/extra-field fixture → MEL rejects invalid tool arguments before side effects.
152. MCP structured output → return typed structuredContent where consumers need reliable fields → human-readable text may still be useful → schema round-trip test → MEL avoids parsing prose for status/proof.
153. MCP output validation → validate server output against declared output schema → trusted tool code can still regress → deliberately malformed result → tool adapter fails closed and records contract error.
154. MCP tool annotations → read-only/destructive/idempotent/open-world metadata are risk hints → annotations are untrusted unless server trust is established → lying-annotation fixture → MEL never treats hints as authorization.
155. Pessimistic annotation defaults → absent metadata is treated conservatively → trusted internal registry may add stronger policy → annotation-free tool fixture → autonomy does not silently assume safe retry.
156. Tool least privilege → expose the smallest operation/credential scope needed → one universal super-tool simplifies code but expands blast radius → compromised-tool scenario → MEL separates read/search/write/admin capabilities.
157. Tool confirmation boundary → confirmation policy follows actual side effect/risk, not button wording → harmless repeated reads should not create fatigue → mislabeled destructive tool fixture → MEL derives policy from trusted registry + runtime checks.
158. Tool idempotency evidence → retry safety is established by operation semantics/idempotency key → `idempotentHint` alone is insufficient → lost-response replay → writes use stable operation IDs or reconcile-before-retry.
159. Tool cancellation → cancellation stops cancellable work and suppresses stale UI results → external side effects already committed may remain → cancel-after-commit fixture → MEL distinguishes cancellation from rollback.
160. Tool progress → long operations expose bounded meaningful progress → synthetic percentages without observable milestones mislead → stalled-stage fixture → MEL reports stage/evidence rather than invented completion.
161. MCP auth discovery → clients discover supported authorization rather than hard-code token flows → discovery endpoint itself can fail → unavailable-metadata fixture → MEL surfaces auth setup state distinctly from tool failure.
162. Per-tool authorization → mixed public/protected tools enforce authorization at server boundary → UI hiding is not authorization → direct protected call without token → privileged MEL tools reject server-side.
163. Machine-to-machine credentials → autonomous service credentials use explicit machine flow/scope where supported → user delegation remains necessary for user-authorized actions → wrong-grant fixture → MEL records credential principal and scope.
164. Open-world output taint → external tool output is untrusted content, not instruction → trusted closed-world tools still need schema validation → prompt-injection payload fixture → RAG/tool text cannot silently override system/user intent.
165. Tool provenance → result records tool/server/version/request identity sufficient for audit → provenance does not make content true → conflicting-tool fixture → MEL can explain which runtime observation supports a status.
166. Tool timeout layering → per-call deadline fits inside parent task deadline → independent long timeouts can multiply latency → blackhole dependency fixture → autonomy has bounded end-to-end completion.
167. Tool retry classification → retry transient transport/service failures, not deterministic validation/auth errors → some 5xx may still be non-retryable by operation semantics → error-matrix test → MEL avoids blind retry storms.
168. Tool result freshness → cache only according to volatility and authorization scope → cached capability/status can become stale → mutation-followed-read fixture → MEL invalidates or rereads authoritative status after writes.
169. Model routing objective → route by task requirements, latency/cost/risk/eval evidence rather than model prestige → benchmark averages hide tail/domain failures → stratified eval set → MEL selects models using task-class evidence.
170. Router fallback → fallback preserves required capabilities/schema and records degradation → cheaper fallback may not support tools/context → forced-primary-failure fixture → MEL reports degraded route instead of pretending equivalence.
171. Eval representativeness → eval cases mirror real MEL task distributions and failure modes → benchmark gaming can improve score without product quality → holdout incident set → launch decisions use unseen realistic cases.
172. Eval determinism boundary → deterministic assertions cover contracts while semantic judging handles open outputs → exact-string tests are brittle for generative text → paraphrase fixture → MEL separates structural from semantic gates.
173. Eval contamination → keep holdouts isolated from optimization prompts/data → small public benchmarks invite overfitting → canary holdout → claimed improvement must generalize.
174. Grounded status eval → status answers must be entailed by current runtime evidence → historical assistant messages are not runtime truth → stale-success transcript fixture → MEL answers current state from authoritative observation.
175. Contradiction eval → latest explicit user constraint outranks older conflicting conversational assumptions → runtime safety constraints remain higher priority → seeded contradiction conversation → MEL resolves provenance/recency correctly.
176. Ellipsis resolution eval → short follow-ups bind to active subject and retained constraints → ambiguous multi-subject context may require clarification → `c'est bon ?` after two jobs fixture → MEL names what was actually checked.
177. D1 replica awareness → read replicas are asynchronous; Sessions provide sequential consistency → unconstrained reads may legitimately lag → write/read fixture across session modes → MEL uses Sessions when read-your-own-write matters.
178. D1 bookmarks → propagate bookmark when continuity across logical sessions matters → bookmark does not replace transaction/business invariant → chained request fixture → state-changing UI can reread at least the version it just observed.
179. D1 first-primary choice → request latest primary state only where freshness requires it → forcing primary everywhere sacrifices replica benefit → stale-sensitive vs read-only benchmark → MEL chooses freshness deliberately.
180. D1 indexing → index frequent predicates/joins/uniqueness based on measured query plans → indexes add write/storage cost → EXPLAIN before/after → MEL optimizes hot queries from evidence.
181. D1 row-cost awareness → returned-row count is not scanned-row count → pagination alone cannot fix unindexed scans → query meta/plan gate → diagnostics flag expensive hot scans.
182. D1 recovery → Time Travel enables minute-level point-in-time recovery window → availability of restore feature is not a rehearsed recovery → isolated restore drill → migration gate includes tested restore procedure.
183. Migration reversibility → destructive schema/data migrations require forward/backout or restore plan → some migrations cannot be cheaply reversed → seeded migration failure → candidate never promotes on unproven recovery.
184. R2 strong object consistency → direct binding/S3 reads observe completed object writes/deletes globally → cached custom-domain delivery can remain stale → binding-vs-cache fixture → MEL uses authoritative R2 read for proof.
185. R2 last-writer risk → concurrent writes to same key can overwrite by completion order → strong consistency does not prevent logical lost update → competing writers fixture → mutable manifests use versioned keys/preconditions/coordination.
186. R2 listing → listings are strongly consistent but paginated/limited → one page is not complete inventory → >1000 object fixture → backup/audit enumerators traverse cursors.
187. R2 multipart resume → resume handle creation need not validate upload existence → obtaining a handle is not proof → invalid uploadId fixture → MEL verifies operation result before status PASS.
188. R2 cache invalidation → overwrite/delete behind cache may serve old/negative cached content → direct storage remains authoritative → cached 404 then upload fixture → UI/CDN proof accounts for purge/TTL.
189. Queue delivery semantics → consumers assume redelivery and design effects idempotently → guaranteed delivery is not exactly-once effect → ack-loss fixture → jobs deduplicate by stable operation key.
190. Queue batch retry → one failed message can cause batch redelivery unless successes are explicitly acknowledged → whole-batch retry magnifies duplicates → partial failure fixture → MEL acknowledges successful independent items.
191. Queue DLQ → poison messages move to observable DLQ after bounded retries → DLQ without consumer/alert is delayed data loss → poison fixture → autonomy exposes DLQ count/replay path.
192. Queue retry budget → retries consume operations and time; cap with backoff/classification → zero retries can lose transient work → persistent dependency failure → MEL bounds retries and records terminal cause.
193. Queue concurrency → max concurrency is a load-control knob, not correctness mechanism → low concurrency cannot fix non-idempotent code → saturation fixture → MEL tunes throughput only after correctness.
194. Durable Object alarm semantics → alarms are at-least-once and handlers must be idempotent → single scheduled alarm does not imply single execution → duplicate alarm fixture → scheduled entity work reconciles before effect.
195. Durable Object alarm multiplexing → one DO alarm can drive many stored due events → periodic wakeups with no work waste resources → sparse schedule fixture → MEL schedules next due event rather than constant churn.
196. Durable Object ownership → use per-entity serialization where invariants benefit → global singleton can bottleneck unrelated work → concurrent entities benchmark → MEL shards coordination by invariant boundary.
197. Workflow step durability → make each step a self-contained retryable unit with persisted outputs → giant steps reduce resumability/diagnosis → injected mid-pipeline failure → MEL resumes from proven checkpoint.
198. Workflow idempotent calls → external/API/binding effects inside retryable steps must tolerate replay → workflow durability does not make third-party effects exactly once → lost-step-ack fixture → side effects carry idempotency/reconciliation.
199. Workflow human approval → durable pause/approval is explicit state → approval UI alone cannot substitute server state → refresh/restart during pause → MEL preserves approval boundary across runtime restarts.
200. Build provenance attestation → attest released executable artifacts and verify provenance policy → attestation proves origin/build linkage, not software safety → wrong-SHA artifact verification → MEL release evidence binds artifact to exact candidate SHA and verification result.

## Deduplication / transfer
This block extends earlier generic timeout/idempotency/security lessons with MCP contract/risk semantics, model-routing/eval falsifiability, D1 consistency/recovery, R2 consistency/cache distinctions, Queue redelivery/DLQ, DO alarm semantics, Workflows retry boundaries and release provenance. Reused concepts count only where a new platform-specific failure mode and falsifiable gate are added. Repository search found no transfer package; none ingested.

## Next exact work
Continue at `MEL_CROSS_DOMAIN_EXPERT_PLUS_cycle_201/10000`; prioritize Cloudflare security/API/release supply chain, observability and frontend performance measurement, then inspect candidate code for an open defect where these lessons can become validated XP. Before any write, reread candidate HEAD and reconcile if changed. Production remains untouched.