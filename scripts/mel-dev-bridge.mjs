import http from 'node:http'; import {LocalDevBridge} from '../src/dev/dev-bridge.js'; import {DevJobService} from '../src/dev/dev-job-service.js';
const token=process.env.MEL_DEV_BRIDGE_TOKEN; if(!token) throw new Error('MEL_DEV_BRIDGE_TOKEN is required');
const bridge=new LocalDevBridge({repoRoot:process.cwd()}), jobs=new DevJobService();
const json=(res,status,body)=>{res.writeHead(status,{'content-type':'application/json'});res.end(JSON.stringify(body));};
const server=http.createServer(async(req,res)=>{try{if(req.headers.authorization!==`Bearer ${token}`)return json(res,401,{error:'BRIDGE_AUTH_REQUIRED'});const url=new URL(req.url,'http://localhost');const body=await new Promise(resolve=>{let s='';req.on('data',d=>s+=d);req.on('end',()=>resolve(s?JSON.parse(s):{}))});if(req.method==='GET'&&url.pathname==='/status')return json(res,200,{online:true,branch:await bridge.currentBranch()});if(req.method==='GET'&&url.pathname==='/jobs')return json(res,200,{jobs:jobs.list()});if(req.method==='POST'&&url.pathname==='/jobs'){const j=jobs.create(body);return json(res,201,j);}const m=url.pathname.match(/^\/jobs\/([^/]+)\/(claim|rollback|test)$/);if(m){const [,id,op]=m;if(op==='claim')return json(res,200,jobs.claim(id));if(op==='rollback')return json(res,200,await bridge.rollbackCandidate(id));if(op==='test')return json(res,200,await bridge.runTests(body.command,id));}return json(res,404,{error:'NOT_FOUND'});}catch(e){json(res,e.code==='BRIDGE_AUTH_REQUIRED'?401:400,{error:e.code||e.message});}});server.listen(Number(process.env.MEL_DEV_BRIDGE_PORT||8788),'127.0.0.1',()=>console.log('MEL_DEV_BRIDGE_ONLINE'));
