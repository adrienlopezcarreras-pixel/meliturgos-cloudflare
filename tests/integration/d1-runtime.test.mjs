import test from 'node:test'; import assert from 'node:assert/strict'; import { sqliteD1 } from '../helpers/sqlite-d1.mjs'; import { D1RuntimeRepositories } from '../../src/core/orchestrator/d1-runtime.js';
test('D1 runtime repositories survive service re-instantiation', async()=>{const db=sqliteD1(); try {const a=new D1RuntimeRepositories(db); const e=await a.knowledgeGraph.entity({type:'PROJECT',name:'Orion',source:'test',confidence:1}); await a.timeline.append({type:'DECISION',title:'x',description:'x',occurred_at:1,source:'test',confidence:1}); const job=await a.automations.create({type:'EVENT',actions:[]}); await a.automations.enable({id:job.id}); await a.automations.run({id:job.id,result:{ok:true}}); const b=new D1RuntimeRepositories(db); assert.equal((await b.knowledgeGraph.query({type:'PROJECT'}))[0].name,'Orion'); assert.equal((await b.timeline.list()).length,1); assert.equal((await b.automations.history({id:job.id})).length,1);} finally{db.close()}});
